# Flagrum.Launcher

A simple Windows console application. Ironically, only used by the Linux version of Flagrum.

The purpose of this application is to run in the WINE/Proton prefix for FFXV Windows Edition
so that it can launch the game and inject the mod loader, similar to what Flagrum does
natively on Windows without this application.